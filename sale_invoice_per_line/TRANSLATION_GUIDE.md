# دليل الترجمة - Translation Guide
## sale_invoice_per_line Module

### ✅ الملفات المحدّثة (Updated Files):

1. **`i18n/ar.po`** - ملف الترجمة العربية الكامل
2. **`i18n/sale_invoice_per_line.pot`** - ملف القالب للترجمة
3. **`models/sale_order.py`** - تم تحديثه لاستخدام `_()` مع جميع النصوص
4. **`models/account_move.py`** - تم تحديثه لاستخدام `_()` مع جميع النصوص

---

## 📋 الترجمات الموجودة في `ar.po`:

### حقول Sale Order (Sale Order Fields):
| English | العربية |
|---------|---------|
| Invoice Per Line | فوترة كل سطر بشكل منفصل |

### حقول Invoice/Account Move (Invoice Fields):
| English | العربية |
|---------|---------|
| Sale Order Line | سطر أمر البيع |
| Installments | عدد الأقساط |
| First Payment | الدفعة الأولى |

### رسائل المساعدة (Help Messages):
| English | العربية |
|---------|---------|
| If enabled, 'Create Invoice' will generate a separate invoice for each invoiceable order line. | إذا تم تفعيله، فإن 'إنشاء فاتورة' سينشئ فاتورة منفصلة لكل سطر قابل للفوترة في الطلب. |

### رسائل الأخطاء (Error Messages):
| English | العربية |
|---------|---------|
| Error creating invoice for line %s: %s | خطأ في إنشاء فاتورة للسطر %s: %s |

### نماذج النظام (System Models):
| English | العربية |
|---------|---------|
| Sales Order | أمر البيع |
| Journal Entry | قيد يومية |
| Journal Item | بند قيد اليومية |

---

## 🚀 كيفية تطبيق الترجمات (How to Apply Translations):

### الطريقة 1: من واجهة Odoo (مفضّلة)

1. **أعد تشغيل خادم Odoo**
   ```bash
   sudo systemctl restart odoo
   # أو
   python3 odoo-bin -c odoo.conf
   ```

2. **حدّث الموديول**
   - افتح Odoo في المتصفح
   - اذهب إلى: **Apps** (التطبيقات)
   - فعّل Developer Mode: **Settings → Activate the developer mode**
   - ابحث عن `sale_invoice_per_line`
   - انقر على ⋮ (ثلاث نقاط) → **Upgrade**

3. **استورد الترجمة العربية**
   - اذهب إلى: **Settings → Translations → Import Translation**
   - اختر:
     - **Language**: Arabic / العربية
     - **File**: اختر ملف `i18n/ar.po`
     - **☑ Overwrite Existing Terms**
   - انقر **Import**

4. **امسح الكاش (Cache)**
   - Settings → Technical → Database Structure → **Clear Assets & Cache**
   - أو اضغط **Ctrl+Shift+R** في المتصفح

5. **تحقق من النتيجة**
   - غيّر لغة المستخدم إلى العربية
   - افتح أي Sale Order
   - يجب أن تظهر "فوترة كل سطر بشكل منفصل" ✅
   - افتح أي Invoice
   - يجب أن تظهر حقول "عدد الأقساط" و "الدفعة الأولى" ✅

---

## 📝 وصف الموديول (Module Description):

هذا الموديول يسمح بـ:
- **إنشاء فواتير منفصلة لكل سطر** في أمر البيع
- **نقل معلومات الأقساط** من السطر إلى الفاتورة
- **تتبع الدفعة الأولى وعدد الأقساط** على مستوى الفاتورة

### الحقول المخصصة (Custom Fields):

#### في Sale Order:
- **invoice_per_line**: خيار لتفعيل الفوترة المنفصلة لكل سطر

#### في Account Move (Invoice):
- **sale_order_line_id**: ربط الفاتورة بسطر محدد من أمر البيع
- **installment_num**: عدد الأقساط
- **first_payment**: قيمة الدفعة الأولى

---

## 🔧 التخصيصات التقنية (Technical Customizations):

### 1. Override `_create_invoices()` في Sale Order:
- عند تفعيل `invoice_per_line`، يتم إنشاء فاتورة منفصلة لكل سطر
- ينقل قيم `installment_num` و `first_payment` من السطر إلى الفاتورة

### 2. Views المعدلة:
- **sale_order_form_invoice_per_line**: إضافة حقل `invoice_per_line` في نموذج أمر البيع
- **account_move_inherit_view**: إضافة حقول `installment_num` و `first_payment` في نموذج الفاتورة

---

## ⚠️ نصائح مهمة (Important Tips):

1. **استخدم دائماً `_()`** مع النصوص في الكود Python:
   ```python
   string=_("Invoice Per Line")  # ✅ صحيح
   string="Invoice Per Line"      # ❌ خطأ - لن يترجم
   ```

2. **في ملفات XML**، النصوص في `string=""` تترجم تلقائياً:
   ```xml
   <field name="invoice_per_line"/>  <!-- يترجم من الموديل تلقائياً -->
   ```

3. **بعد أي تعديل**:
   - أعد تشغيل Odoo
   - حدّث الموديول
   - استورد الترجمة من جديد
   - امسح الكاش

4. **تأكد من إعداد اللغة**:
   - Settings → Users → اختر المستخدم
   - Language: **Arabic / العربية**

---

## 🔍 إنشاء ملف .pot جديد (Generate New .pot File):

إذا أضفت نصوصاً جديدة في الكود وتريد تحديث ملف الترجمة:

```bash
cd /home/odoo/PycharmProjects/odoo18/odoo18

# إنشاء ملف .pot جديد
python3 odoo-bin -c odoo.conf -d DATABASE_NAME --i18n-export=custom_frtz/FRTZ/sale_invoice_per_line/i18n/sale_invoice_per_line.pot --modules=sale_invoice_per_line --stop-after-init
```

---

## 📧 الاعتمادات (Dependencies):

هذا الموديول يعتمد على:
- `base` - قاعدة Odoo
- `sale` - موديول المبيعات
- `account` - موديول المحاسبة
- `pricelist_expression` - موديول قوائم الأسعار
- `account_invoice_installments` - موديول أقساط الفواتير

تأكد من تثبيت وترجمة جميع الموديولات المعتمد عليها.

---

## 🎯 حالات الاستخدام (Use Cases):

### مثال 1: أمر بيع بثلاثة منتجات
- المنتج A: 1000 ريال، 5 أقساط، دفعة أولى 200
- المنتج B: 500 ريال، 3 أقساط، دفعة أولى 100
- المنتج C: 800 ريال، نقداً

عند تفعيل **فوترة كل سطر بشكل منفصل**:
- ✅ سيتم إنشاء 3 فواتير منفصلة
- ✅ كل فاتورة تحتوي على معلومات الأقساط الخاصة بها
- ✅ يمكن متابعة كل منتج وأقساطه بشكل مستقل

### مثال 2: أمر بيع عادي
- عند عدم تفعيل الخيار، يعمل النظام بالطريقة الافتراضية
- ✅ فاتورة واحدة لجميع السطور

---

**آخر تحديث**: 2025-11-10  
**إصدار Odoo**: 18.0+e  
**إصدار الموديول**: 1.0.0

