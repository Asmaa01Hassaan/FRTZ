# دليل الترجمة - Translation Guide
## invoice_installment_extension Module

### ✅ الملفات المحدّثة (Updated Files):

1. **`i18n/ar.po`** - ملف الترجمة العربية الكامل (570+ ترجمة)
2. **`i18n/invoice_installment_extension.pot`** - ملف القالب للترجمة
3. **جميع ملفات models/** - تم تحديثها لاستخدام `_()` مع جميع النصوص:
   - `models/account_move.py`
   - `models/account_payment.py`
   - `models/account_payment_term.py`
   - `models/control_payment.py`
   - `models/installment_list.py`
   - `models/payment_invoice_to_pay.py`
   - `models/payment_records.py`
   - `models/res_partner.py`
   - `models/res_config_settings.py`

---

## 📋 ملخص الترجمات:

هذا الموديول يحتوي على **570+ ترجمة** تغطي:
- 11 موديل (Models)
- 80+ حقل (Fields)
- 50+ رسالة خطأ وتحذير
- 20+ رسالة نجاح ومعلومات
- جميع حالات الأقساط (Installment States)

---

## 🎯 الموديلات المترجمة (Translated Models):

### 1. **Sale Order (أمر البيع)**
- Hide Buttons Setting → إعداد إخفاء الأزرار

### 2. **Account Move (الفاتورة/قيد اليومية)**
- Number of Installments → عدد الأقساط
- First Payment Amount → مبلغ الدفعة الأولى
- To Pay → للدفع
- Amount to Pay → المبلغ المستحق للدفع
- View Generate → عرض التوليد

### 3. **Account Payment (الدفعة)**
- Date for Pay → تاريخ الدفع
- Available Invoices → الفواتير المتاحة
- Selected Invoices → الفواتير المحددة
- Invoice Payment Lines → بنود دفع الفواتير
- Control Payments → مراقبة الدفعات
- Total Invoice To Pay → إجمالي الفواتير للدفع
- Remaining Amount → المبلغ المتبقي
- Total Control To Pay → إجمالي المراقبة للدفع
- Control Remaining Amount → المبلغ المتبقي للمراقبة

### 4. **Account Payment Term (شروط الدفع)**
- Is Installment Payment Term → هو شرط دفع بالتقسيط
- Number of Installments → عدد الأقساط
- First Payment Percentage → نسبة الدفعة الأولى

### 5. **Control Payment (التحكم بالدفع)**
- Payment → الدفع
- Invoice → الفاتورة
- Transaction → المعاملة
- Transaction Date → تاريخ المعاملة
- Total Amount → المبلغ الإجمالي
- Remaining Amount → المبلغ المتبقي
- Due Amount → المبلغ المستحق
- Due Date → تاريخ الاستحقاق
- To Pay → للدفع

### 6. **Installment List (قائمة الأقساط)**
- Installment Reference → مرجع القسط
- Sequence → التسلسل
- Payment Term → شرط الدفع
- Amount → المبلغ
- Due Date → تاريخ الاستحقاق
- Paid Date → تاريخ الدفع
- Paid Amount → المبلغ المدفوع
- Remaining Amount → المبلغ المتبقي
- Status → الحالة

#### حالات القسط (Installment States):
- Pending → قيد الانتظار
- Partial Paid → مدفوع جزئياً
- Paid → مدفوع
- Overdue → متأخر
- Cancelled → ملغي

### 7. **Payment Invoice To Pay (فاتورة الدفع المستحقة)**
- Transaction → المعاملة
- Transaction Date → تاريخ المعاملة
- Total Amount → المبلغ الإجمالي
- To Pay Amount → المبلغ للدفع

### 8. **Payment Records (سجلات الدفع)**
- Payment Record Reference → مرجع سجل الدفع
- Payment Name → اسم الدفعة
- Payment Date → تاريخ الدفع

### 9. **Res Partner (الشريك/العميل)**
- Installment List → قائمة الأقساط
- Has Installments → لديه أقساط
- View Installments → عرض الأقساط
- Total Installments → إجمالي الأقساط
- Paid Installments → الأقساط المدفوعة
- Partial Paid Installments → الأقساط المدفوعة جزئياً
- Pending Installments → الأقساط المعلقة
- Overdue Installments → الأقساط المتأخرة
- Total Installment Amount → إجمالي مبلغ الأقساط
- Total Paid Amount → إجمالي المبلغ المدفوع
- Remaining Amount → المبلغ المتبقي

### 10. **Res Config Settings (إعدادات النظام)**
- Hide Buttons → إخفاء الأزرار
- Hide 'Send by Email' & 'Preview' buttons on Sales Orders → إخفاء أزرار 'إرسال بالبريد الإلكتروني' و 'معاينة' في أوامر المبيعات

---

## 📝 رسائل الأخطاء والتحذيرات المترجمة:

### رسائل Sale Order:
- "Sending quotations is disabled by system configuration." → "إرسال عروض الأسعار معطل بواسطة إعدادات النظام."
- "Changing state to 'Quotation Sent' is disabled by system configuration." → "تغيير الحالة إلى 'تم إرسال عرض السعر' معطل بواسطة إعدادات النظام."

### رسائل Account Move:
- "Please set the number of installments first" → "الرجاء تحديد عدد الأقساط أولاً"
- "No payment term found for this invoice" → "لم يتم العثور على شرط دفع لهذه الفاتورة"
- "Installment list already exists for this invoice" → "قائمة الأقساط موجودة بالفعل لهذه الفاتورة"

### رسائل Account Payment:
- "Please select at least one invoice to pay" → "الرجاء تحديد فاتورة واحدة على الأقل للدفع"
- "Please select a date for pay" → "الرجاء تحديد تاريخ للدفع"
- "Total invoice to pay amount (%s) must equal the payment amount (%s)." → "إجمالي مبلغ الفواتير للدفع (%s) يجب أن يساوي مبلغ الدفع (%s)."
- "Payment processed for %d invoice(s) successfully." → "تمت معالجة الدفع لـ %d فاتورة بنجاح."

### رسائل Installment List:
- "Only pending or partial paid installments can be marked as fully paid" → "يمكن فقط وضع علامة على الأقساط المعلقة أو المدفوعة جزئياً كمدفوعة بالكامل"
- "Fully paid installments cannot be updated" → "لا يمكن تحديث الأقساط المدفوعة بالكامل"
- "Cancelled installments cannot be paid" → "لا يمكن دفع الأقساط الملغاة"
- "Partial payment amount must be greater than 0" → "مبلغ الدفع الجزئي يجب أن يكون أكبر من 0"
- "Paid amount cannot exceed the installment amount" → "المبلغ المدفوع لا يمكن أن يتجاوز مبلغ القسط"
- "Please enter a valid amount to pay" → "الرجاء إدخال مبلغ صحيح للدفع"
- "No installments found due on or before the selected date" → "لم يتم العثور على أقساط مستحقة في أو قبل التاريخ المحدد"
- "Payment has been distributed to installments successfully." → "تم توزيع الدفعة على الأقساط بنجاح."

### رسائل Payment Invoice To Pay:
- "Invoice is required. Please select an invoice." → "الفاتورة مطلوبة. الرجاء تحديد فاتورة."
- "Invoice is required. Cannot remove invoice from payment line." → "الفاتورة مطلوبة. لا يمكن إزالة الفاتورة من بند الدفع."

---

## 🚀 كيفية تطبيق الترجمات (How to Apply Translations):

### الطريقة 1: من واجهة Odoo (مفضّلة)

1. **أعد تشغيل خادم Odoo**
   ```bash
   sudo systemctl restart odoo
   # أو
   python3 odoo-bin -c odoo.conf
   ```

2. **حدّث الموديول**
   - افتح Odoo في المتصفح
   - اذهب إلى: **Apps** (التطبيقات)
   - فعّل Developer Mode: **Settings → Activate the developer mode**
   - ابحث عن `invoice_installment_extension`
   - انقر على ⋮ (ثلاث نقاط) → **Upgrade**

3. **استورد الترجمة العربية**
   - اذهب إلى: **Settings → Translations → Import Translation**
   - اختر:
     - **Language**: Arabic / العربية
     - **File**: اختر ملف `i18n/ar.po`
     - **☑ Overwrite Existing Terms**
   - انقر **Import**

4. **امسح الكاش (Cache)**
   - Settings → Technical → Database Structure → **Clear Assets & Cache**
   - أو اضغط **Ctrl+Shift+R** في المتصفح

5. **تحقق من النتيجة**
   - غيّر لغة المستخدم إلى العربية
   - افتح أي Invoice أو Payment
   - يجب أن تظهر جميع النصوص مترجمة ✅

---

## 📝 وصف الموديول (Module Description):

هذا الموديول يوفر نظام شامل لإدارة الأقساط والدفعات في Odoo:

### الميزات الأساسية:

1. **إدارة الأقساط على مستوى الفاتورة**:
   - تحديد عدد الأقساط
   - تحديد مبلغ الدفعة الأولى
   - إنشاء شروط دفع تلقائياً
   - إنشاء قائمة أقساط تلقائياً

2. **نظام التحكم بالدفعات**:
   - اختيار فواتير متعددة للدفع
   - توزيع الدفعة على الفواتير
   - حساب المبلغ المستحق بناءً على تاريخ محدد
   - متابعة المبالغ المدفوعة والمتبقية

3. **قائمة الأقساط (Installment List)**:
   - عرض جميع الأقساط لكل فاتورة
   - حالات الأقساط: معلق، مدفوع جزئياً، مدفوع، متأخر، ملغي
   - حساب الأقساط المستحقة بناءً على التاريخ
   - توزيع الدفعات تلقائياً على الأقساط

4. **سجلات الدفع (Payment Records)**:
   - تتبع جميع الدفعات لكل قسط
   - سجل كامل بتاريخ الدفع والمبلغ
   - ربط الدفعات بالأقساط والفواتير

5. **تقارير على مستوى الشريك**:
   - عدد الأقساط الإجمالي
   - الأقساط المدفوعة
   - الأقساط المعلقة
   - الأقساط المتأخرة
   - إجمالي المبالغ المدفوعة والمتبقية

---

## 🔧 التخصيصات التقنية (Technical Customizations):

### 1. Override Methods:
- `account.move.create()` - إنشاء شروط دفع وأقساط تلقائياً
- `account.move.write()` - تحديث الأقساط عند تغيير البيانات
- `account.move.action_post()` - إنشاء قائمة أقساط عند تأكيد الفاتورة
- `account.payment.action_post()` - معالجة الدفعات تلقائياً
- `installment.list.action_mark_paid()` - وضع علامة مدفوع على القسط
- `installment.list.action_mark_partial_paid()` - دفع جزئي للقسط

### 2. Computed Fields:
- `account.move.due_amount` - حساب المبلغ المستحق بناءً على تاريخ محدد
- `account.move.total_remaining_amount` - حساب المبلغ المتبقي من الأقساط
- `installment.list.remaining_amount` - حساب المبلغ المتبقي للقسط
- `installment.list.is_late` - تحديد إذا كان القسط متأخر
- `installment.list.days_overdue` - حساب أيام التأخير

### 3. Wizards:
- `payment.term.generation.wizard` - معالج إنشاء شروط الدفع
- `payment.invoice.wizard` - معالج دفع الفواتير

### 4. Cron Jobs:
- `installment.list._cron_check_overdue_installments()` - فحص الأقساط المتأخرة تلقائياً

---

## ⚠️ نصائح مهمة (Important Tips):

1. **استخدم دائماً `_()`** مع النصوص في الكود Python:
   ```python
   string=_("Number of Installments")  # ✅ صحيح
   string="Number of Installments"      # ❌ خطأ - لن يترجم
   ```

2. **في ملفات XML**، النصوص في `string=""` تترجم تلقائياً من الموديل

3. **بعد أي تعديل على الترجمات**:
   - أعد تشغيل Odoo
   - حدّث الموديول
   - استورد الترجمة من جديد
   - امسح الكاش

4. **تأكد من إعداد اللغة**:
   - Settings → Users → اختر المستخدم
   - Language: **Arabic / العربية**

5. **للتحقق من الترجمات**:
   - افتح أي نموذج في الموديول
   - غيّر اللغة إلى العربية
   - تحقق من ظهور الترجمات

---

## 🔍 إنشاء ملف .pot جديد (Generate New .pot File):

إذا أضفت نصوصاً جديدة في الكود وتريد تحديث ملف الترجمة:

```bash
cd /home/odoo/PycharmProjects/odoo18/odoo18

# إنشاء ملف .pot جديد
python3 odoo-bin -c odoo.conf -d DATABASE_NAME \\
  --i18n-export=custom_frtz/FRTZ/invoice_installment_extension/i18n/invoice_installment_extension.pot \\
  --modules=invoice_installment_extension \\
  --stop-after-init
```

---

## 📧 الاعتمادات (Dependencies):

هذا الموديول يعتمد على:
- `base` - قاعدة Odoo
- `account` - موديول المحاسبة
- `sale` - موديول المبيعات
- `account_invoice_installments` - موديول أقساط الفواتير
- `pricelist_expression` - موديول قوائم الأسعار
- `sale_invoice_per_line` - موديول فوترة كل سطر
- `frtz_customer` - موديول عملاء FRTZ

تأكد من تثبيت وترجمة جميع الموديولات المعتمد عليها.

---

## 📊 إحصائيات الترجمة (Translation Statistics):

- **إجمالي الترجمات**: 570+
- **Models مترجم**: 11
- **Fields مترجم**: 80+
- **Error Messages مترجم**: 50+
- **Success Messages مترجم**: 20+
- **States/Selections مترجم**: 5
- **Wizards مترجم**: 2

---

## 🎯 حالات الاستخدام (Use Cases):

### مثال 1: فاتورة بـ 5 أقساط
- المبلغ الإجمالي: 10,000 ريال
- عدد الأقساط: 5
- الدفعة الأولى: 2,000 ريال
- الأقساط المتبقية: 4 × 2,000 ريال

النظام سيقوم بـ:
- ✅ إنشاء شرط دفع تلقائياً
- ✅ إنشاء 5 أقساط في قائمة الأقساط
- ✅ حساب تواريخ الاستحقاق تلقائياً
- ✅ تتبع حالة كل قسط

### مثال 2: دفعة على عدة فواتير
- الدفعة: 5,000 ريال
- الفواتير: 3 فواتير بأقساط مختلفة
- التاريخ المحدد: 2025-11-15

النظام سيقوم بـ:
- ✅ حساب المبلغ المستحق من كل فاتورة حتى التاريخ المحدد
- ✅ توزيع الدفعة على الأقساط المستحقة
- ✅ تحديث حالة الأقساط تلقائياً
- ✅ إنشاء سجلات دفع لكل قسط

---

## 🐛 استكشاف الأخطاء (Troubleshooting):

### المشكلة: الترجمات لا تظهر
**الحل**:
1. تحقق من أن اللغة العربية مفعّلة في Odoo
2. أعد استيراد ملف ar.po مع خيار Overwrite
3. امسح الكاش
4. أعد تشغيل Odoo

### المشكلة: بعض الحقول لا تترجم
**الحل**:
1. تحقق من أن الحقل يستخدم `_()` في الكود
2. تحقق من وجود الترجمة في ملف ar.po
3. حدّث الموديول
4. أعد استيراد الترجمات

### المشكلة: رسائل الأخطاء بالإنجليزية
**الحل**:
1. تحقق من أن الرسالة تستخدم `_()` في الكود
2. استورد ملف ar.po من جديد
3. امسح الكاش

---

**آخر تحديث**: 2025-11-10  
**إصدار Odoo**: 18.0+e  
**إصدار الموديول**: 1.0.0  
**عدد الملفات المحدثة**: 9 models + 2 i18n files

